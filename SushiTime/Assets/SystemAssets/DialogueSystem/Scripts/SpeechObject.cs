namespace DialogueSystem
{
    using System.Collections;
    using System.Collections.Generic;
    using UnityEngine;

    /// <summary>
    /// Defines the behaviours for the speech bubbles.
    /// </summary>
    public class SpeechObject : MonoBehaviour
    {
        // A speech object needs to know the following:
        // What character name.
        // It must resize the rectTransform to fit the character name.

        // What speech will show up?
        // Resize the the speech bubble accordingly.
        // However, the speechbubble MUST have a max, and a min size too.

        // Speech bubble must understand if it's left or right orientated.
        // Default should be centered.
    } 
}
